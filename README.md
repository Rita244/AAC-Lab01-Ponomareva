OC: Microsoft Windows NT 10.0.19045.0
64-битная ОС: True
Логических процессоров: 8
PID процесса: 20260
Память процесса: 15 МБ

# Практическая работа №1

## Цель
Изучить способы получения информации о среде выполнения программы (компьютере, ОС, процессах) с помощью возможностей языка C# и платформы .NET.

## 1. Информация о компьютере
*   **Имя компьютера: DESKTOP-1FSG1P4
*   **Пользователь: ASUS
*   **Дата и время: 15.0.2026 21:35

## 2. Информация об операционной системе
*   **Версия ОС (OSVersion): Microsoft Windows NT 10.0.19045.0
*   **64-битная ОС (Is64BitOperatingSystem): True 
*   **Сравнение с Параметрами Windows: 64-разрядная операционная система, процессор x64

## 3. Логические процессоры
*   **Гипотеза: Я предполагала, что программа покажет 8 логических процессоров
*   **Результат программы: 8
*   **Результат в Диспетчере задач: 8
*   Числа совпадают

## 4. Память процесса
*   **Результат WorkingSet: 15 МБ
*   **Сравнение с Диспетчером задач: 16,2 МБ.

## Контрольные вопросы
1.  **Почему программа может узнать имя компьютера, хотя вы не писали его в коде?**
    Программа обращается к операционной системе через специальные библиотеки (API). ОС хранит эти сведения о себе и предоставляет их программе по запросу.
2.  **Какую роль играет операционная система, когда программа запрашивает у неё сведения?**
    ОС выступает посредником. Она управляет ресурсами компьютера и предоставляет программам доступ к системной информации (железо, настройки, переменные среды).
3.  **Что означает «64-битная ОС»?**
    Это означает, что операционная система может обрабатывать данные разрядностью 64 бита за один такт и адресовать более 4 ГБ оперативной памяти.
4.  **Что показывает Environment.ProcessorCount и обязательно ли это число совпадает с числом физических ядер?**
    Показывает количество логических процессоров. Если  на процессоре включена технология Hyper-Threading, одно физическое ядро может представляться системе как два логических, то не обязательно
5.  **Чем память процесса (WorkingSet) отличается от всей оперативной памяти компьютера?**
    WorkingSet — это объем физической памяти (ОЗУ), который в данный момент выделен конкретно для этого процесса. Вся оперативная память — это общий объем ОЗУ, доступный для всех программ и самой ОС.
6.  **Почему одна и та же программа без изменений в коде может показать разные результаты на разных компьютерах?**
   Потому что программа считывает параметры среды, в которой она запущена. Разные компьютеры имеют разное железо (процессоры, память) и разные версии операционных систем.

## Итоговый вывод
В ходе практической работы я научилась использовать класс `Environment` для получения данных о компьютере и операционной системе. Выяснила, что программа не хранит эти данные в себе, а запрашивает их у ОС в момент выполнения. Именно поэтому один и тот же код на разных компьютерах выдает разные результаты: он адаптируется под текущую среду выполнения. Понятие «память процесса» отличается от общей оперативной памяти, т.к. показывает ресурсы, выделенные конкретной запущенной программе


## Дополнительное задание. Мини-монитор системы

### Что делает программа

Программа сначала собирает и печатает общие данные о компьютере (имя, пользователь, версия Windows, количество ядер). А затем она выводит свой номер (PID) и три раза подряд замеряет, сколько оперативной памяти она занимает в данный момент. Между замерами она делает паузу, чтобы   увидеть изменения

### Что нового я узнал о классе Process

Я узнала, что в C# есть специальный класс Process, который позволяет программе "общаться" с процессами Windows. С его помощью можно узнать номер текущего процесса (Id) и сколько памяти он "съел" (WorkingSet64). А метод Refresh() нужен для того, чтобы программа не показывала старые (закешированные) данные, а каждый раз запрашивала у Windows свежие цифры.

### Сравнение памяти программы и Диспетчера задач

Цифры в программе и в Диспетчере задач могут немного не совпадать (например, 17 МБ против 18 МБ). потому что Windows и .NET считают память по-разному. Главное — понять, что обе цифры показывают память одного конкретного процесса (у которого одинаковый PID), а не всей оперативной памяти компьютера.

### Почему значение памяти может немного изменяться

Память процесса не стоит на месте. Пока программа работает, она создает новые объекты, что-то вычисляет, обращается к своим библиотекам. Из-за этого память может тоувеличиваться, то уменьшаться. Плюс в C# есть "сборщик мусора", который периодически очищает ненужные данные, освобождая память. Поэтому три замера подряд могут показать разные числаgit add .